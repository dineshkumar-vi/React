import { loadMain, loadMainSuccess, loadMainError } from "../Actions";
import * as types from "../../../Shared/ActionTypes";

describe("Unit test for loadMain ()", () => {
  // this case is to test the expected object is returned
  });
  // case is to test the success scenario of get benefits
  it("loadMainSuccess ", () => {
  });
  // case is to test the invalid scenario of benefits
  it("loadMainError ", () => {
  });
});
